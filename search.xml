<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>深入解析 Cloudflare Pages 域名解析功能</title>
      <link href="/2026/06/21/cloudflare-pages-dns/"/>
      <url>/2026/06/21/cloudflare-pages-dns/</url>
      
        <content type="html"><![CDATA[<h1 id="🌐-Cloudflare-Pages-域名解析功能完全指南"><a href="#🌐-Cloudflare-Pages-域名解析功能完全指南" class="headerlink" title="🌐 Cloudflare Pages 域名解析功能完全指南"></a>🌐 Cloudflare Pages 域名解析功能完全指南</h1><blockquote><p>在成功将博客部署到 Cloudflare Pages 后，绑定自己的域名是让网站显得专业的关键一步。本文将深入浅出地解释 Cloudflare Pages 的域名解析功能，帮助你透彻理解其工作原理并顺利完成配置。</p></blockquote><hr><h2 id="📌-什么是域名解析？为什么需要它？"><a href="#📌-什么是域名解析？为什么需要它？" class="headerlink" title="📌 什么是域名解析？为什么需要它？"></a>📌 什么是域名解析？为什么需要它？</h2><p>域名解析（DNS Resolution）是将人类易记的域名（如 <code>blog.example.com</code>）转换为计算机可识别的 IP 地址的过程。对于 Cloudflare Pages 而言，域名解析意味着：</p><ul><li>将你的自定义域名指向 Cloudflare 的边缘网络。</li><li>让用户通过你的专属域名访问 Pages 上托管的网站。</li><li>利用 Cloudflare 的全球 CDN 加速访问速度，并享受自动 SSL 加密。</li></ul><p>简单来说，<strong>没有正确的域名解析，你的域名就无法打开你的网站内容</strong>。</p><hr><h2 id="🔧-Cloudflare-Pages-支持两种-DNS-配置方式"><a href="#🔧-Cloudflare-Pages-支持两种-DNS-配置方式" class="headerlink" title="🔧 Cloudflare Pages 支持两种 DNS 配置方式"></a>🔧 Cloudflare Pages 支持两种 DNS 配置方式</h2><p>根据你的域名当前 DNS 托管位置，Cloudflare Pages 提供了两种接入方式。</p><h3 id="方式一：使用-Cloudflare-DNS（完全托管）"><a href="#方式一：使用-Cloudflare-DNS（完全托管）" class="headerlink" title="方式一：使用 Cloudflare DNS（完全托管）"></a>方式一：使用 Cloudflare DNS（完全托管）</h3><ul><li><strong>适用场景</strong>：你的域名注册商支持修改 NS 记录，且你希望将所有 DNS 管理交给 Cloudflare。</li><li><strong>操作流程</strong>：<ol><li>在 Cloudflare 仪表盘添加你的域名，并按照提示将域名的 NS 记录修改为 Cloudflare 提供的地址。</li><li>等待 NS 生效后，在 Cloudflare Pages 项目的“自定义域”中添加你的域名（例如 <code>blog.example.com</code>）。</li><li><strong>Cloudflare 会自动为你创建所需的 DNS 记录（如 CNAME 或 A 记录）</strong>，无需手动操作。</li></ol></li><li><strong>优点</strong>：<ul><li>全自动管理，减少出错。</li><li>享受 Cloudflare 全套安全功能（如 DDoS 防护、WAF）。</li><li>可在 Cloudflare 控制台统一管理所有子域名的解析。</li></ul></li></ul><h3 id="方式二：使用外部-DNS-提供商（保持现有-DNS）"><a href="#方式二：使用外部-DNS-提供商（保持现有-DNS）" class="headerlink" title="方式二：使用外部 DNS 提供商（保持现有 DNS）"></a>方式二：使用外部 DNS 提供商（保持现有 DNS）</h3><ul><li><strong>适用场景</strong>：你的域名注册商不支持修改 NS，或者你希望继续使用当前的 DNS 管理服务。</li><li><strong>操作流程</strong>：<ol><li>在 Cloudflare Pages 项目中添加自定义域名。</li><li>Cloudflare 会提供一条 <strong>CNAME 记录</strong> 的目标值（例如 <code>你的项目名.pages.dev</code>）。</li><li>登录你的域名注册商（或 DNS 管理面板），手动添加一条 CNAME 记录：<ul><li>主机记录：填写子域名前缀（如 <code>blog</code>，若为裸域名则留空或填写 <code>@</code>，见下文说明）。</li><li>记录值：填写 Cloudflare 提供的 <code>项目名.pages.dev</code> 地址。</li></ul></li><li>保存后等待 DNS 生效。</li></ol></li><li><strong>注意事项</strong>：如果你要添加的是 <strong>裸域名（根域名，如 <code>example.com</code>）</strong>，请务必阅读下一节的“CNAME 展平”说明。</li></ul><hr><h2 id="🧩-关键概念：CNAME-展平（CNAME-Flattening）"><a href="#🧩-关键概念：CNAME-展平（CNAME-Flattening）" class="headerlink" title="🧩 关键概念：CNAME 展平（CNAME Flattening）"></a>🧩 关键概念：CNAME 展平（CNAME Flattening）</h2><h3 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h3><p>在 DNS 标准中，<strong>裸域名（zone apex，即 <code>example.com</code> 形式）不能直接设置 CNAME 记录</strong>，因为裸域名通常需要同时存在其他类型的记录（如 MX 邮件记录、SOA 记录等），而 CNAME 记录与这些记录冲突。</p><h3 id="Cloudflare-的解决方案"><a href="#Cloudflare-的解决方案" class="headerlink" title="Cloudflare 的解决方案"></a>Cloudflare 的解决方案</h3><p>Cloudflare 通过 <strong>CNAME 展平</strong> 技术巧妙地解决了这个问题：</p><ul><li>当你为裸域名添加 CNAME 记录时，Cloudflare 的 DNS 服务器<strong>不会返回 CNAME 响应</strong>，而是主动解析该 CNAME 目标（即 <code>pages.dev</code> 的 IP 地址），并直接返回 <strong>A 记录（IPv4）和 AAAA 记录（IPv6）</strong>。</li><li>这样既满足了裸域名必须指向 IP 的协议要求，又能让你的裸域名成功指向 Cloudflare Pages 项目。</li></ul><h3 id="对用户的影响"><a href="#对用户的影响" class="headerlink" title="对用户的影响"></a>对用户的影响</h3><ul><li>你<strong>无需进行任何额外操作</strong>，Cloudflare 会自动处理。</li><li>这意味着你可以轻松地将 <code>example.com</code> 直接绑定到你的 Pages 项目，而不必强制使用 <code>www.example.com</code> 这类子域名。</li></ul><hr><h2 id="🔒-自动-SSL-证书：让网站更安全"><a href="#🔒-自动-SSL-证书：让网站更安全" class="headerlink" title="🔒 自动 SSL 证书：让网站更安全"></a>🔒 自动 SSL 证书：让网站更安全</h2><p>绑定域名后，Cloudflare Pages 会通过 <strong>Let’s Encrypt</strong> 或 Cloudflare 自家的 CA，为你的域名<strong>自动申请并部署免费的 SSL&#x2F;TLS 证书</strong>。</p><ul><li><strong>颁发时间</strong>：通常只需几分钟，证书状态可在 Pages 项目的“自定义域”页面查看。</li><li><strong>证书类型</strong>：支持单域名证书和泛域名证书（如 <code>*.example.com</code>）。</li><li><strong>自动续期</strong>：证书到期前系统会自动续期，无需人工干预。</li><li><strong>验证方法</strong>：在浏览器访问你的域名，查看地址栏是否出现小锁图标，且证书信息显示有效。</li></ul><hr><h2 id="✅-验证与故障排查"><a href="#✅-验证与故障排查" class="headerlink" title="✅ 验证与故障排查"></a>✅ 验证与故障排查</h2><h3 id="如何确认域名解析成功？"><a href="#如何确认域名解析成功？" class="headerlink" title="如何确认域名解析成功？"></a>如何确认域名解析成功？</h3><ul><li>在 Cloudflare Pages 项目的“自定义域”列表中，查看域名状态是否为 <strong>“Active”（活跃）</strong>。</li><li>使用 <code>ping</code> 命令检查域名是否解析为 Cloudflare 的 IP 地址（通常是 <code>104.16.x.x</code> 或 <code>104.17.x.x</code> 等）。</li><li>直接在浏览器访问 <code>https://你的域名</code>，确认能否正常显示你的网站。</li></ul><h3 id="常见问题及解决方法"><a href="#常见问题及解决方法" class="headerlink" title="常见问题及解决方法"></a>常见问题及解决方法</h3><table><thead><tr><th>问题现象</th><th>可能原因</th><th>解决步骤</th></tr></thead><tbody><tr><td>域名状态长时间显示 <strong>“Pending”</strong></td><td>DNS 记录未正确添加或未生效</td><td>检查 CNAME 记录是否填写正确，等待 5~30 分钟再刷新；可尝试清除本地 DNS 缓存（<code>ipconfig /flushdns</code>）</td></tr><tr><td>访问时显示 <strong>Cloudflare 404 页面</strong></td><td>Pages 项目未部署或构建失败</td><td>检查 GitHub 仓库源码是否完整，查看 Cloudflare Pages 的部署日志</td></tr><tr><td>访问时出现 <strong>400 Bad Request</strong></td><td>使用了 HTTP 访问而服务端仅支持 HTTPS</td><td>将 <code>http://</code> 改为 <code>https://</code>，或在 Cloudflare 开启“始终使用 HTTPS”</td></tr><tr><td>SSL 证书状态显示 <strong>“错误”</strong></td><td>证书颁发失败</td><td>确认域名解析正确，等待一段时间自动重试；若仍失败，可尝试重新绑定域名</td></tr><tr><td>裸域名（<code>example.com</code>）无法访问</td><td>外部 DNS 提供商不支持 CNAME 展平</td><td>建议将 DNS 迁移到 Cloudflare，或改用 <code>www</code> 子域名</td></tr></tbody></table><hr><h2 id="📝-实操示例：将-blog-naranja33-top-绑定到我的项目"><a href="#📝-实操示例：将-blog-naranja33-top-绑定到我的项目" class="headerlink" title="📝 实操示例：将 blog.naranja33.top 绑定到我的项目"></a>📝 实操示例：将 <code>blog.naranja33.top</code> 绑定到我的项目</h2><p>以下是我成功配置的完整步骤，供参考：</p><ol><li><strong>拥有一个域名</strong>（我使用了免费域名 <code>naranja33.top</code>，通过 DNSHE 获取）。</li><li><strong>在 Cloudflare Pages 项目中添加自定义域</strong>：<ul><li>进入 Pages 项目 → 设置 → 自定义域 → 设置自定义域。</li><li>输入 <code>blog.naranja33.top</code>。</li></ul></li><li><strong>选择 DNS 配置方式</strong>：<ul><li>我选择使用 <strong>外部 DNS 提供商</strong>（因为免费域名不支持修改 NS）。</li><li>Cloudflare 给出提示，要求添加 CNAME 记录。</li></ul></li><li><strong>在 DNS 管理面板添加 CNAME</strong>：<ul><li>登录 DNSHE 控制台，添加记录：<ul><li>主机名：<code>blog</code></li><li>目标：<code>my-studio-hexo.pages.dev</code>（替换为你的项目名）</li></ul></li></ul></li><li><strong>等待生效</strong>（约 5 分钟后），Cloudflare 显示状态变为 <strong>Active</strong>。</li><li><strong>访问</strong> <code>https://blog.naranja33.top</code>，成功显示博客页面，且地址栏有小锁图标。</li></ol><hr><h2 id="🎯-总结"><a href="#🎯-总结" class="headerlink" title="🎯 总结"></a>🎯 总结</h2><p>Cloudflare Pages 的域名解析功能将繁琐的 DNS 配置自动化，通过两种灵活的方式（托管 DNS 或外部 DNS）让你轻松绑定自定义域名。其 CNAME 展平技术解决了裸域名无法使用 CNAME 的长期痛点，而自动 SSL 证书则保障了网站的安全传输。</p><p>无论你是技术新手还是资深运维，都能在几分钟内完成域名绑定，让博客或网站拥有专属标识。如果遇到问题，利用上述排查方法，绝大多数情况都能快速解决。</p><hr><p><strong>我的博客地址</strong>：<a href="https://blog.naranja33.top/">https://blog.naranja33.top</a><br><strong>欢迎留言交流</strong>，或通过 GitHub 联系我。</p>]]></content>
      
      
      <categories>
          
          <category> 技术 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Cloudflare </tag>
            
            <tag> DNS </tag>
            
            <tag> 域名解析 </tag>
            
            <tag> 教程 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
